
<%= simple_form_for @state do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, f.object.new_record? ? 'Add State' : 'Edit State', class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
